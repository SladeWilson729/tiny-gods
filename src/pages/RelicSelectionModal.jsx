// ... keep existing code ...

  const handleSkip = () => {
    // In custom deck mode, we need to navigate to progression instead of showing card modal
    onRelicSelected(null);
  };

// ... keep existing code ...